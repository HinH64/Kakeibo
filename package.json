{
  "name": "kakeibo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.0",
  "description": "Multi-currency personal finance application",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild"]
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "hono": "^4.7.0",
    "@kakeibo/core": "workspace:*",
    "@kakeibo/server": "workspace:*",
    "drizzle-orm": "^0.39.3",
    "postgres": "^3.4.5",
    "uuid": "^11.1.0"
  }
}
